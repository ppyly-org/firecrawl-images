# Detached upstream source is reproducibly fetched, but never committed.
/vendor/firecrawl/
/.generated/
/.test-upstream-pin/

# Hermetic builder inputs and local container-engine state are never source.
/.cache/
/.docker/
/.docker-buildx/
/.buildx/
/roots/
/layers/
/tars/
/*.tar
/*.tar.gz
/*.tar.zst
/*.deb
/*.oci
/*.log
/*.token
/*.tokens
/.env
/.env.*
/prototype/
/prototypes/
__pycache__/
